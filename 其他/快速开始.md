# 🚀 5 分钟快速部署到 GitHub

## 第一步：创建 GitHub 仓库（2分钟）

1. 访问 https://github.com 并登录
2. 点击右上角 **+** → **New repository**
3. 填写：
   - 仓库名：`my-html-project`（任意名字）
   - ✅ 选择 **Public**
   - ❌ 不要勾选 README
4. 点击 **Create repository**

## 第二步：复制仓库地址（10秒）

创建后，GitHub 会显示一个地址，类似：
```
https://github.com/你的用户名/my-html-project.git
```
**复制这个地址，稍后要用！**

## 第三步：在终端执行命令（2分钟）

打开终端（Terminal），依次执行：

```bash
# 1. 进入项目目录
cd "/Users/qiushengming/Desktop/李慧雯设计素材/cursor test"

# 2. 初始化 Git
git init

# 3. 添加文件
git add .

# 4. 提交
git commit -m "首次提交"

# 5. 连接 GitHub（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/my-html-project.git

# 6. 推送
git branch -M main
git push -u origin main
```

**注意**：第 6 步会要求输入用户名和密码：
- 用户名：你的 GitHub 用户名
- 密码：使用 **Personal Access Token**（不是登录密码）

### 如何获取 Token：
1. 访问 https://github.com/settings/tokens
2. 点击 **Generate new token** → **Generate new token (classic)**
3. 勾选 `repo` 权限
4. 点击 **Generate token**
5. **复制 token**（只显示一次！）
6. 在终端粘贴这个 token 作为密码

## 第四步：启用 GitHub Pages（1分钟）

1. 在 GitHub 仓库页面，点击 **Settings**
2. 左侧菜单找到 **Pages**
3. 设置：
   - Source: **Deploy from a branch**
   - Branch: **main**
   - Folder: **/ (root)**
4. 点击 **Save**

## 第五步：访问你的网站（30秒）

等待 1-2 分钟后，访问：
```
https://你的用户名.github.io/my-html-project/定时管道优化/字段映射批量操作.html
```

🎉 **完成！**

---

## 💡 小贴士

### 如果想直接访问根目录就显示页面：

将 HTML 文件复制为 `index.html`：

```bash
cd "/Users/qiushengming/Desktop/李慧雯设计素材/cursor test"
cp "定时管道优化/字段映射批量操作.html" index.html
git add index.html
git commit -m "添加主页"
git push
```

然后访问：`https://你的用户名.github.io/my-html-project/`

### 后续更新文件：

```bash
cd "/Users/qiushengming/Desktop/李慧雯设计素材/cursor test"
git add .
git commit -m "更新内容"
git push
```

---

## ❓ 遇到问题？

查看详细教程：`GitHub部署保姆级教程.md`

